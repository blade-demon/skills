# Case 3 Expected Signatures — 指数精选（含 Tab 切换）

```text
## index-fund-section.png
T: title + meta + action
M: nav -> list(card(title -> meta + action + action))
B: -
O: -
F: -
notes: { tab_active=港股指数 }
```

Expected Step 6 decision: single image, one independent component.
